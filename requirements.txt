# =====================================================================================
# ILM Geo-INQUIRE Dashboard — Python dependencies
# =====================================================================================
# Pinning notes:
#   * kaleido is pinned to 0.2.1 because it is the last release in the 0.x series
#     and is the most-tested combination with Plotly 5.x / 6.x. The 1.x series
#     requires Plotly >= 6.1.1; without that exact pairing, PNG export silently
#     breaks and Streamlit prints a confusing UserWarning at every startup.
#   * Everything else uses lower bounds only — Streamlit Cloud will resolve to
#     the latest compatible release at build time.
# =====================================================================================

streamlit>=1.28,<2
plotly>=5.0.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
streamlit-option-menu>=0.3.0
kaleido==0.2.1
openpyxl>=3.0.0
gspread>=5.0.0
oauth2client>=4.1.0
